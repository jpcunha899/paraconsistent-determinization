# paraconsistent-determinization
This script implements the determinization method for FPNA whose transition are taken from the unit square [0,1]^2. It includes the examples of Section 3 and prints the resulting deterministic automaton (FPDA).
